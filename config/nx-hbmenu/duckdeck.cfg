// DuckDeck layout settings. Restart hbmenu after saving changes.
duckdeck={
    // 4 columns by default. Supported range: 1 to 6.
    columns=4;

    // Icon width/height in pixels. Supported range: 48 to 256.
    iconSize=96;
};
